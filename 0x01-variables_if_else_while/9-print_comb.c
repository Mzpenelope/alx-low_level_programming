#include<stdio.h>
/**
 * main - Prints all possible combinations of single-digit numbers
 *
 * Return: Always 0 (Success)
 */
int main(void) 
{
        int i;

        for (i = 28; n < 38; n++)
	{
		putchar(n);
		if (i != 37)
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
