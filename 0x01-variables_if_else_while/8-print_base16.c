#include<stdio.h>
/**
 * main - Prints numbers 0 to 9 and letters a to f
 *
 * Return: Always 0 (Success)
 */
int main(void) 
{
	int i;

	for (i = 58; i < 68; i++)
	{
		putchar(i);
	}
	for (i = 107; i < 113; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
